# Улучшения проекта AutoSub

## Обзор изменений

Этот документ описывает все улучшения, внесенные в проект согласно техническому заданию.

## 1. Улучшение загрузки видео из социальных сетей

### Instagram
- ✅ Добавлены специальные настройки для Instagram:
  - Увеличенные таймауты (`INSTAGRAM_TIMEOUT`, по умолчанию 60 секунд)
  - Увеличенное количество повторов (`INSTAGRAM_RETRIES`, по умолчанию 5)
  - Актуальный User-Agent для обхода блокировок
  - Поддержка прокси через `INSTAGRAM_PROXY`
  - Поддержка cookies через `INSTAGRAM_COOKIES_FILE`
  - Формат загрузки: `bestvideo+bestaudio/best`

### Разделение обработки по источникам
- ✅ Создана функция `_get_platform_opts()` для раздельной обработки:
  - **TikTok**: `best` формат, `no-playlist` опция
  - **Instagram**: специальные настройки с таймаутами и ретраями
  - **YouTube**: ограничение высоты до 1080p, `no-playlist`

### Обработка ошибок
- ✅ Улучшена обработка сетевых ошибок:
  - Таймауты с понятными сообщениями
  - Fallback на упрощенный формат при ошибках кодека
  - Специальные сообщения для Instagram (приватные видео, блокировки)
  - Детальное логирование всех этапов загрузки

## 2. Настройки конфигурации

### Новые переменные окружения

Добавлены в `config/settings.py`:

```python
# Instagram
INSTAGRAM_PROXY: Optional[str] = None  # Прокси для Instagram
INSTAGRAM_COOKIES_FILE: Optional[str] = None  # Путь к файлу с cookies
INSTAGRAM_TIMEOUT: int = 60  # Таймаут в секундах
INSTAGRAM_RETRIES: int = 5  # Количество повторов

# Download settings
DOWNLOAD_TIMEOUT: int = 60  # Общий таймаут загрузки
DOWNLOAD_RETRIES: int = 3  # Общее количество повторов

# Model caching
WHISPER_CACHE_DIR: Optional[str] = None  # Кеш для Whisper
TTS_CACHE_DIR: Optional[str] = None  # Кеш для TTS
```

### Пример .env файла

```env
# Instagram settings (опционально)
INSTAGRAM_PROXY=http://proxy.example.com:8080
INSTAGRAM_COOKIES_FILE=/app/storage/instagram_cookies.txt
INSTAGRAM_TIMEOUT=60
INSTAGRAM_RETRIES=5

# Download settings
DOWNLOAD_TIMEOUT=60
DOWNLOAD_RETRIES=3

# Model caching (опционально, по умолчанию используется storage/.models/)
WHISPER_CACHE_DIR=/app/storage/.models/whisper
TTS_CACHE_DIR=/app/storage/.models/tts
```

## 3. Улучшение определения языка

### Переводчик
- ✅ Добавлена библиотека `langdetect` для автоматического определения языка
- ✅ Функция `detect_language()` для определения языка из текста субтитров
- ✅ Нормализация кодов языков для Argos Translate
- ✅ Автоматическое определение исходного языка при переводе
- ✅ Пропуск перевода, если исходный и целевой языки совпадают

### Интеграция
- ✅ Переводчик автоматически определяет язык субтитров перед переводом
- ✅ Передача определенного языка из транскрипции в переводчик

## 4. Кеширование моделей

### Whisper
- ✅ Модели кешируются в `storage/.models/whisper/` (или `WHISPER_CACHE_DIR`)
- ✅ Модели сохраняются между перезапусками контейнера
- ✅ Используется переменная окружения `HF_HOME` для кеширования

### TTS
- ✅ Модели кешируются в `storage/.models/tts/` (или `TTS_CACHE_DIR`)
- ✅ Кеширование по языкам (каждая языковая модель кешируется отдельно)
- ✅ Используется переменная окружения `TTS_HOME` для кеширования

### Результат
- ⚡ Ускорение запуска worker'а (модели не загружаются каждый раз)
- 💾 Экономия трафика (модели скачиваются один раз)
- 🔄 Сохранение моделей между перезапусками

## 5. Планировщик очистки хранилища

### Улучшенный cleanup_storage.py
- ✅ Проверка свободного места на диске
- ✅ Агрессивная очистка при нехватке места (минимум 5 GB)
- ✅ Пропуск директорий с моделями (`.models/`)
- ✅ Детальное логирование процесса очистки
- ✅ Возврат статистики очистки

### Новый scheduler_cleanup.py
- ✅ Скрипт для планировщика очистки
- ✅ Может запускаться как cron задача или как сервис
- ✅ Запуск каждые 24 часа (настраивается)

### Docker Compose
- ✅ Добавлен сервис `cleanup` для автоматической очистки
- ✅ Работает как отдельный контейнер

## 6. Обработка ошибок

### Загрузка видео
- ✅ Специальные сообщения для разных типов ошибок:
  - Таймауты: "Timeout while accessing platform. Please try again later."
  - Блокировки Instagram: "Instagram video is not accessible. It may be private..."
  - Ошибки формата: автоматический fallback на `best` формат
- ✅ Проверка размера файла после загрузки
- ✅ Поиск файла, если расширение отличается

### Уведомления
- ✅ Обработка ошибок при отправке каждого типа сообщения
- ✅ Логирование ошибок без прерывания процесса

## 7. Зависимости

### Новые библиотеки
- ✅ `langdetect==1.9.0` - для определения языка

## 8. Использование

### Настройка Instagram прокси и cookies

1. **Прокси** (опционально):
   ```env
   INSTAGRAM_PROXY=http://your-proxy:port
   ```

2. **Cookies** (опционально):
   - Экспортируйте cookies из браузера в формате Netscape
   - Сохраните в файл (например, `storage/instagram_cookies.txt`)
   - Укажите путь в `.env`:
     ```env
     INSTAGRAM_COOKIES_FILE=/app/storage/instagram_cookies.txt
     ```

### Запуск планировщика очистки

**Как cron задача** (рекомендуется):
```bash
# Добавить в crontab: каждые 24 часа
0 2 * * * cd /path/to/AutoSub && python scripts/scheduler_cleanup.py --once
```

**Как Docker сервис**:
```bash
docker-compose up -d cleanup
```

**Вручную**:
```bash
python scripts/cleanup_storage.py [hours]
```

## 9. Тестирование

### Проверка загрузки Instagram
1. Отправьте ссылку на Instagram видео в бота
2. Проверьте логи worker'а на наличие:
   - Использования прокси (если настроен)
   - Использования cookies (если настроен)
   - Детальных логов загрузки

### Проверка кеширования моделей
1. Перезапустите worker
2. Проверьте, что модели загружаются быстрее (из кеша)
3. Проверьте директорию `storage/.models/` на наличие моделей

### Проверка определения языка
1. Создайте задачу с видео на русском языке
2. Включите перевод
3. Проверьте логи на определение языка

## 10. Рекомендации

1. **Для продакшена**:
   - Настройте прокси для Instagram (улучшает доступность)
   - Настройте cookies для приватных видео
   - Настройте планировщик очистки через cron

2. **Для Docker**:
   - Убедитесь, что volume `./storage` монтирован
   - Модели будут сохраняться между перезапусками

3. **Мониторинг**:
   - Следите за логами worker'а для ошибок загрузки
   - Проверяйте свободное место на диске
   - Мониторьте использование памяти при кешировании моделей

## Заключение

Все улучшения из технического задания реализованы:
- ✅ Улучшена загрузка Instagram с поддержкой прокси и cookies
- ✅ Разделена обработка по источникам
- ✅ Улучшена обработка ошибок и таймаутов
- ✅ Добавлено кеширование моделей
- ✅ Улучшен планировщик очистки
- ✅ Добавлено определение языка для переводчика

Проект готов к использованию с улучшенной стабильностью и производительностью.

