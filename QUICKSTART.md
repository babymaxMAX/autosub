# 🚀 Быстрый старт AutoSub

Запустите бота за 5 минут!

## Шаг 1: Предварительные требования

✅ Docker Desktop установлен и запущен  
✅ У вас есть токен Telegram бота (получите у [@BotFather](https://t.me/BotFather))  
✅ Свободно минимум 2GB RAM

## Шаг 2: Настройка

1. **Скачайте проект:**
```bash
git clone <repository-url>
cd AutoSub
```

2. **Создайте файл `.env`:**
```bash
# Windows
copy .env.example .env

# Linux/Mac
cp .env.example .env
```

3. **Отредактируйте `.env`:**
Откройте `.env` в любом текстовом редакторе и измените:

```env
# ОБЯЗАТЕЛЬНО измените это!
BOT_TOKEN=8181035994:AAGe8mQDBxRV1Zj0xFCCF-iuhZGk9s3HxRo
ADMIN_IDS=123456789  # Ваш Telegram ID (получите у @userinfobot)

# Измените пароль БД (рекомендуется)
DB_PASSWORD=ваш_надежный_пароль
```

## Шаг 3: Запуск

### Windows:
```bash
run.bat
```

### Linux/Mac:
```bash
chmod +x run.sh
./run.sh
```

### Или вручную через Docker:
```bash
docker-compose up -d
```

## Шаг 4: Проверка

1. **Проверьте, что все контейнеры запущены:**
```bash
docker-compose ps
```

Вы должны увидеть 5 контейнеров в статусе "Up":
- `autosub_bot`
- `autosub_worker`
- `autosub_webhook`
- `autosub_postgres`
- `autosub_redis`

2. **Посмотрите логи:**
```bash
docker-compose logs -f bot
```

Вы должны увидеть:
```
Bot started successfully!
```

## Шаг 5: Тестирование

1. Откройте Telegram и найдите вашего бота
2. Отправьте `/start`
3. Отправьте короткое видео (до 60 сек)
4. Выберите опции и дождитесь результата!

## 🎉 Готово!

Ваш бот AutoSub работает!

## Команды управления

```bash
# Остановить бота
docker-compose down

# Перезапустить
docker-compose restart

# Посмотреть логи
docker-compose logs -f

# Посмотреть использование ресурсов
docker stats

# Обновить бота
git pull
docker-compose up -d --build
```

## 🔧 Настройка тарифов и платежей

Для активации платежей через Platega:

1. Зарегистрируйтесь на [Platega](https://platega.com)
2. Создайте проект
3. Добавьте данные в `.env`:
```env
PLATEGA_API_ID=ваш_api_id
PLATEGA_API_KEY=ваш_api_key
PLATEGA_PROJECT_ID=ваш_project_id
```

4. Настройте webhook в личном кабинете Platega:
```
URL: https://ваш-домен.com/webhook/payment
Method: POST
```

## 📚 Дополнительная информация

- [README.md](README.md) - Полная документация
- [GETTING_STARTED.md](GETTING_STARTED.md) - Детальное руководство
- [PROJECT_SUMMARY.md](PROJECT_SUMMARY.md) - Обзор архитектуры

## ❓ Проблемы?

### Бот не отвечает
```bash
# Проверьте логи
docker-compose logs bot

# Убедитесь, что токен правильный
# Проверьте, что БД доступна
docker-compose exec postgres psql -U autosub -c "SELECT 1;"
```

### Worker не обрабатывает видео
```bash
# Проверьте логи worker
docker-compose logs worker

# Проверьте Redis
docker-compose exec redis redis-cli ping
```

### Недостаточно памяти
```bash
# Увеличьте лимиты в docker-compose.yml
# Или используйте меньшую модель Whisper:
# В .env измените WHISPER_MODEL=tiny
```

## 🆘 Поддержка

Если ничего не помогло:
1. Создайте issue на GitHub
2. Приложите логи: `docker-compose logs > logs.txt`
3. Укажите версию Docker: `docker --version`

---

**Время запуска:** ~5-10 минут  
**Требования:** Docker, 2GB RAM, 5GB диска  
**Сложность:** ⭐⭐☆☆☆ (Легко)

