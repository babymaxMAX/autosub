# AutoSub - Telegram Bot для автоматической обработки видео

Telegram-бот для добавления субтитров, перевода и озвучки к видео с автоматической обработкой.

## 🎯 Возможности

- 🎤 **Распознавание речи** - автоматическое создание субтитров с помощью Faster-Whisper
- 🌐 **Перевод** - перевод субтитров на 50+ языков с использованием Argos Translate
- 🗣️ **Озвучка** - генерация голосовой дорожки с помощью TTS
- 📺 **Хардсаб** - встраивание субтитров в видео
- 📱 **Вертикальный формат** - конвертация видео в формат 9:16 для соцсетей
- 🔗 **Поддержка ссылок** - YouTube, TikTok, Instagram
- 💎 **Система тарифов** - FREE, PRO, CREATOR с различными лимитами
- 💳 **Платежи** - интеграция с Platega для монетизации

## 📋 Требования

- Python 3.11+
- PostgreSQL 15+
- Redis 7+
- FFmpeg
- Docker & Docker Compose (опционально)

## 🚀 Быстрый старт

### Вариант 1: Docker Compose (рекомендуется)

1. Клонируйте репозиторий:
```bash
git clone <repository-url>
cd AutoSub
```

2. Настройте переменные окружения:
```bash
cp .env.example .env
# Отредактируйте .env файл
```

3. Запустите проект:
```bash
docker-compose up -d
```

4. Проверьте статус:
```bash
docker-compose ps
docker-compose logs -f bot
```

### Вариант 2: Локальная установка

1. Создайте виртуальное окружение:
```bash
python -m venv venv
source venv/bin/activate  # Linux/Mac
# или
venv\Scripts\activate  # Windows
```

2. Установите зависимости:
```bash
pip install -r requirements.txt
```

3. Настройте базу данных:
```bash
# Создайте базу данных PostgreSQL
createdb autosub

# Примените миграции
python -c "from db.database import init_db; import asyncio; asyncio.run(init_db())"
```

4. Запустите Redis:
```bash
redis-server
```

5. Запустите компоненты:

```bash
# В отдельных терминалах:

# Бот
python -m bot.main

# Worker
python -m worker.main

# Webhook (опционально)
python -m webhook.main
```

## ⚙️ Конфигурация

Основные параметры настраиваются через файл `.env`:

```env
# Telegram Bot
BOT_TOKEN=your_bot_token_here
ADMIN_IDS=123456789,987654321

# Database
DB_HOST=localhost
DB_PORT=5432
DB_NAME=autosub
DB_USER=autosub
DB_PASSWORD=your_password

# Redis
REDIS_HOST=localhost
REDIS_PORT=6379

# Platega Payment
PLATEGA_API_ID=your_api_id
PLATEGA_API_KEY=your_api_key
PLATEGA_PROJECT_ID=16699

# Processing
WHISPER_MODEL=base  # tiny, base, small, medium, large
WHISPER_DEVICE=cpu  # cpu or cuda
```

## 📦 Структура проекта

```
AutoSub/
├── bot/                    # Telegram бот
│   ├── handlers/          # Обработчики команд
│   ├── services/          # Бизнес-логика
│   ├── keyboards.py       # Клавиатуры
│   ├── middlewares.py     # Middleware
│   └── main.py            # Точка входа
├── worker/                # Worker для обработки видео
│   ├── processors/        # Процессоры (ASR, TTS, etc)
│   ├── tasks.py          # Задачи RQ
│   └── main.py           # Точка входа
├── webhook/               # Webhook сервер для платежей
│   └── main.py
├── db/                    # База данных
│   ├── models.py         # Модели SQLAlchemy
│   ├── database.py       # Подключение к БД
│   └── crud.py           # CRUD операции
├── config/                # Конфигурация
│   ├── settings.py       # Настройки
│   └── constants.py      # Константы
├── storage/               # Хранилище файлов
├── docker-compose.yml     # Docker Compose конфиг
└── requirements.txt       # Python зависимости
```

## 💎 Тарифные планы

### 🆓 FREE
- Видео до 60 секунд
- До 720p
- 3 задачи в день
- С водяным знаком

### 💎 PRO (299₽/мес)
- Видео до 10 минут
- До 1080p
- 50 задач в день
- Без водяного знака
- Перевод на 50+ языков

### ⭐ CREATOR (599₽/мес)
- Видео до 30 минут
- 200 задач в день
- Озвучка субтитров
- Стили субтитров
- Приоритетная обработка

## 🔧 Разработка

### Установка зависимостей для разработки:
```bash
pip install -r requirements.txt
pip install black flake8 pytest pytest-asyncio
```

### Форматирование кода:
```bash
black .
flake8 .
```

### Запуск тестов:
```bash
pytest tests/
```

## 📊 Мониторинг

Для администраторов доступна команда `/admin` с панелью управления:
- 📊 Статистика системы
- 👥 Пользователи
- 📋 Задачи
- 💰 Платежи

## 🐛 Устранение неполадок

### Бот не отвечает
```bash
# Проверьте логи
docker-compose logs -f bot

# Или локально
python -m bot.main
```

### Worker не обрабатывает задачи
```bash
# Проверьте Redis
redis-cli ping

# Проверьте логи worker
docker-compose logs -f worker
```

### Ошибки при обработке видео
```bash
# Проверьте наличие FFmpeg
ffmpeg -version

# Проверьте свободное место
df -h
```

## 📝 Лицензия

MIT License

## 🤝 Поддержка

Если у вас возникли вопросы или проблемы:
- Создайте issue в репозитории
- Напишите в Telegram: @support

## 🔄 Обновления

Следите за обновлениями в репозитории. Для обновления:

```bash
git pull
docker-compose down
docker-compose up -d --build
```

---

Сделано с ❤️ для автоматизации обработки видео

