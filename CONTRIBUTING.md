# Руководство по внесению вклада в AutoSub

Спасибо за интерес к улучшению AutoSub! 

## Как внести вклад

### Сообщение об ошибках

Если вы нашли ошибку:

1. Проверьте, не была ли она уже сообщена в [Issues](https://github.com/your-repo/issues)
2. Создайте новый issue с подробным описанием:
   - Что произошло
   - Что ожидалось
   - Шаги для воспроизведения
   - Версия проекта
   - Логи (если применимо)

### Предложение новых функций

Хотите предложить новую функцию?

1. Создайте issue с меткой "enhancement"
2. Опишите:
   - Проблему, которую решает функция
   - Предлагаемое решение
   - Альтернативы (если есть)

### Pull Requests

1. Форкните репозиторий
2. Создайте ветку для изменений:
   ```bash
   git checkout -b feature/amazing-feature
   ```

3. Внесите изменения

4. Проверьте код:
   ```bash
   # Форматирование
   black .
   
   # Линтер
   flake8 .
   
   # Тесты
   pytest tests/
   ```

5. Закоммитьте изменения:
   ```bash
   git commit -m "Add: Amazing feature"
   ```

6. Отправьте в ваш форк:
   ```bash
   git push origin feature/amazing-feature
   ```

7. Создайте Pull Request

## Стандарты кода

### Python

- Следуйте [PEP 8](https://pep8.org/)
- Используйте type hints
- Документируйте функции docstrings
- Максимальная длина строки: 100 символов

Пример:

```python
def process_video(video_path: str, options: dict) -> str:
    """
    Process video with given options.
    
    Args:
        video_path: Path to input video
        options: Processing options
        
    Returns:
        Path to processed video
    """
    # Implementation
    pass
```

### Commit Messages

Используйте префиксы:

- `Add:` - новые функции
- `Fix:` - исправления
- `Update:` - обновления
- `Remove:` - удаление кода
- `Refactor:` - рефакторинг
- `Docs:` - документация
- `Test:` - тесты

Пример: `Add: Support for new language in translation`

### Тестирование

- Пишите тесты для новых функций
- Убедитесь, что все тесты проходят
- Стремитесь к coverage > 80%

```bash
pytest tests/ --cov=. --cov-report=html
```

## Структура проекта

Ознакомьтесь с [PROJECT_SUMMARY.md](PROJECT_SUMMARY.md) для понимания архитектуры.

## Вопросы?

Не стесняйтесь задавать вопросы в [Discussions](https://github.com/your-repo/discussions)

---

Спасибо за ваш вклад! 🎉

